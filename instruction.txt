1. Создаем директорию проекта
2. Создаем виртуальное окружение(python3 -m venv venv)
3. Создаем файл requirements.txt и записываем туда наши библиотеки
4. Активировать виртуальное окружение(. venv/bin/activate/)
5. Установить зависимости с файла requirements.txt(pip3 install -r requirements.txt)
6. Settings -> Project -> Нажимаем на шестеренку -> Existing environment  -> путо до нашего bin файла
7. Запуск  проекта -> python3 manage.py runserver
8. Запись таблиц в базу данных:
    a. python3 mange.py makemigrations(создает файлы миграции)
    b. python3 mange.py migrate(записывает файлы миграции в базу данных)
9. Запуск приложения для разделение логики проекта -> django-admin startapp product


QuerySet -> запрос в базу данных

all() -> Возвращает все записи

Product.objects.all() -> SELECT * FROM product;

Product.objectss.values() -> Возвращает значения в виде словаря

Product.objects.values_list() -> Возвращает значения в виде списка

MVC

M --> Model 
V --> View(serializers)
C --> Controller(views)

REST - архитектурный стиль написания API приложения 
1. Модель клиент сервер
2. Единобразие интерфейса
3. определение ресурсов(
    URL ('api/v1/products/1/')
)

Для логина используется аутентификация:
    1. Token()
    2. Session()
    3. JWT(Json-web Token)
    4. Base